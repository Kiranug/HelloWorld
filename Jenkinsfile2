@Library('jenkins-shared-library-framework@master') _ //master or whatever branch

pipeline {
    agent any  
    environment {
        // initialize common vars
        common_vars = staging.loadVars()
        } // environment

    stages {
        stage('Some Stage') {
           steps {
                echo "env.DB_URL = ${env.DB_URL}"
              
            }
        }
    }

    post {
        always { 
            script {
                // Print environment variables
                echo "post action"
            } // script
        } // always
    } // post

} // pipeline
